@model PromotionVM
@section Scripts {

    @if (@TempData["notification"] is not null)
    {
        <script>
            toastr.success('@TempData["notification"]')
        </script>
    }
}
<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="mb-0">All Promotion</h5>
            <a href="/Admin/Promotion/Create" class="btn btn-success btn-sm">
                <i class="bi bi-plus-circle me-1"></i> Create
            </a>
        </div>

        <form class="row g-2 mb-3">
            <div class="col-md-4">
                <div class="input-group">
                    <input type="text" class="form-control" name="code" placeholder="Search by code">
                </div>
            </div>
            <div class="col-md-2">
                <button class="input-group-text btn-dark "><i class="bi bi-search"></i></button>
            </div>
        </form>

        <div class="table-responsive scrollbar">
            <table class="table table-bordered table-hover text-center">
                <thead class="table-dark">
                    <tr>
                        <th class="sort">Code</th>
                        <th class="sort">Movie name</th>
                        <th class="sort">Discount</th>
                        <th class="sort">MaxUsage</th>
                        <th class="sort">DeadLine</th>
                        <th class="sort">Action</th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.PromotionModel)
                    {

                        <tr>
                            <td>@item.Code</td>
                            <td>@item.Movie.Name</td>
                            <td>@item.Discount</td>
                            <td>@item.MaxUsage</td>
                            <td>@item.ExpireAt</td>
                            
                            <td>
                                <a href="/Admin/Promotion/Edit/@item.Id"
                                   class="btn btn-warning btn-sm text-white">
                                    <i class="bi bi-pencil-square"></i>
                                </a>

                                <a href="/Admin/Promotion/Delete/@item.Id"
                                   data-url="/Admin/Promotion/Delete/@item.Id"
                                   class="btn btn-danger btn-delete"><i class="bi bi-trash3"></i></a>
                            </td>
                        </tr>

                    }
                </tbody>
            </table>

        </div>
        <div class="card-footer text-center">
            <nav aria-label="Page navigation example">
                <ul class="pagination justify-content-center">
                    @if (Model.CurrentPage > 1)
                    {
                        <li class="page-item ">
                            <a class="page-link" href="/Admin/Promotion/Index?page=@(Model.CurrentPage - 1)">Previous</a>
                        </li>
                    }
                    @for (int i = 1; i <= Model.TotalPages; i++)
                    {
                        <li class="page-item">
                            <a class="page-link" href="/Admin/Promotion/Index?page=@i">@i</a>
                        </li>
                    }
                    @if (Model.CurrentPage < Model.TotalPages)
                    {
                        <li class="page-item">
                            <a class="page-link" href="/Admin/Promotion/Index?page=@(Model.CurrentPage + 1)">Next</a>
                        </li>
                    }
                </ul>
            </nav>

        </div>

        <script>
            document.addEventListener("DOMContentLoaded", function () {

                const swalWithBootstrapButtons = Swal.mixin({
                    customClass: {
                        confirmButton: "btn btn-success ms-2",
                        cancelButton: "btn btn-danger"
                    },
                    buttonsStyling: false
                });

                document.querySelectorAll(".btn-delete").forEach(link => {

                    link.addEventListener("click", function (e) {
                        e.preventDefault();

                        const url = this.getAttribute("href");


                        swalWithBootstrapButtons.fire({
                            title: "Are you sure?",
                            text: "You won't be able to revert this!",
                            icon: "warning",
                            showCancelButton: true,
                            confirmButtonText: "Yes, delete it!",
                            cancelButtonText: "No, cancel!",
                            reverseButtons: true
                        }).then((result) => {

                            if (result.isConfirmed) {
                                window.location.href = url;
                            }

                        });
                    });

                });

            });
        </script>

